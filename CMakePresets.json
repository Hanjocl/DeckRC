{
    "version": 8,
    "configurePresets": [
        {
            "name": "Default Toolchain Configure",
            "displayName": "Default Toolchain Configure",
            "description": "Sets Ninja generator, build and install directory",
            "generator": "Ninja",
            "binaryDir": "${sourceDir}/build/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        }
    ]
}